# Tooltrace-web 开发计划

## 当前项目状态

### 已完成的功能模块
1. ✅ Web 项目骨架搭建完成
2. ✅ 6 步向导 UI 实现完成
3. ✅ 图片上传功能实现
4. ✅ Fabric.js SVG 编辑器（可拖点、缩放、旋转）
5. ✅ 参数面板实现
6. ✅ 状态管理（Zustand）实现

### 当前技术栈
- React 18 + TypeScript + Vite
- Zustand 状态管理
- Fabric.js SVG 编辑器
- Three.js 3D 预览（占位）
- OpenCV.js（已安装，待集成）

## 依赖安全问题及解决方案

### 当前存在的安全警告
在安装依赖时出现以下警告：
```
npm warn deprecated inflight@1.0.6: This module is not supported, and leaks memory.
npm warn deprecated npmlog@5.0.1: This package is no longer supported.
npm warn deprecated rimraf@3.0.2: Rimraf versions prior to v4 are no longer supported.
npm warn deprecated glob@7.2.3: Contains security vulnerabilities.
npm warn deprecated are-we-there-yet@2.0.0: This package is no longer supported.
npm warn deprecated gauge@3.0.2: This package is no longer supported.
npm warn deprecated tar@6.2.1: Contains security vulnerabilities.
```

### 解决方案
1. **定期依赖更新**：
   - 每月执行 `npm outdated` 检查过时依赖
   - 每季度执行 `npm audit` 安全审计
   - 优先更新有安全漏洞的依赖包

2. **依赖升级策略**：
   - 对于主版本号变更的依赖，先在开发分支测试
   - 使用 `npm audit fix` 修复可自动解决的安全问题
   - 对于无法自动修复的问题，寻找替代方案

3. **长期维护计划**：
   - 建立依赖更新日志
   - 设置自动化依赖检查（如 Dependabot）
   - 制定紧急安全补丁响应流程

## 下一步开发计划

### 第一阶段：OpenCV.js 集成（下周）
**目标**：替换假图片处理，实现真实的纸张检测与透视校正

#### 具体任务：
1. 集成 OpenCV.js WASM 版本（替换当前 npm 包）
2. 实现纸张边缘检测算法
3. 实现四边形拟合算法
4. 实现透视校正（Homography）功能
5. 实现 A4/Letter 纸张尺寸标定
6. 添加手动调整四角功能

#### 技术要点：
- 使用 Web Worker 避免阻塞 UI
- 实现图像预处理（灰度化、高斯模糊、边缘检测）
- 实现轮廓查找和多边形逼近
- 计算变换矩阵并应用透视校正

### 第二阶段：SAM 分割集成（再下周）
**目标**：替换假轮廓，实现真实的 AI 工具轮廓提取

#### 具体任务：
1. 集成 SAM ONNX 模型（浏览器端运行）
2. 实现点击生成掩码功能
3. 实现轮廓提取（从掩码到路径）
4. 支持多工具识别
5. 添加模型缓存机制（IndexedDB）

#### 技术要点：
- 使用 ONNX Runtime Web
- 在 Web Worker 中运行模型推理
- 实现点提示（Point Prompts）交互
- 优化模型加载和缓存策略

### 第三阶段：Clipper.js 轮廓偏移（再下周）
**目标**：实现真实的轮廓偏移功能

#### 具体任务：
1. 集成 Clipper2 WASM
2. 实现轮廓向内/外偏移功能
3. 支持 0~2mm 偏移范围调节
4. 实现轮廓布尔运算

#### 技术要点：
- Clipper 算法实现精确的偏移计算
- 支持复杂轮廓的偏移处理
- 处理偏移后的自交问题

### 第四阶段：3D 建模与导出（再下周）
**目标**：实现真实的 3D 模型生成与多种格式导出

#### 具体任务：
1. 集成 OpenCASCADE WASM
2. 实现轮廓拉伸生成 3D 实体
3. 实现布尔运算（腔体挖空、手指槽切割）
4. 实现 Three.js 真实 3D 预览
5. 实现 STL/STEP/3MF/DXF/SVG 导出

#### 技术要点：
- 使用 OpenCASCADE 进行 B-Rep 建模
- 实现几何变换和布尔运算
- 优化 3D 渲染性能
- 支持多种 CAD 格式导出

## 风险评估与应对策略

### 技术风险
1. **WASM 内存限制**：
   - 应对：大模型推理放 Web Worker，复杂场景分步处理

2. **SAM 模型体积大（~100MB）**：
   - 应对：分模型档位（Tiny/Small/Base），按需下载；OPFS 持久缓存

3. **手机广角拍摄透视畸变严重**：
   - 应对：引导页强调"远距离+变焦"；提供 ArUco 角标增强标定方案

### 性能风险
1. **复杂工具（钳子/扳手）投影轮廓不完整**：
   - 应对：允许用户手动绘制补充轮廓；提供"多层嵌件"选项

## 质量保证措施

### 测试策略
1. **单元测试**：
   - 使用 Vitest 进行核心算法测试
   - 测试图像处理和几何计算函数

2. **组件测试**：
   - 使用 React Testing Library
   - 测试 UI 组件交互和状态管理

3. **端到端测试**：
   - 使用 Cypress/Playwright
   - 测试完整用户流程

### 代码质量
1. **TypeScript 类型安全**：
   - 严格类型检查
   - 接口定义清晰

2. **代码规范**：
   - ESLint + Prettier 统一代码风格
   - 提交前代码检查

## 部署与发布计划

### 开发环境
- 本地开发：Vite 开发服务器
- 热重载和快速编译

### 生产部署
- 构建优化：Vite 生产构建
- 静态文件部署到 CDN
- Service Worker 缓存核心资源

### 版本发布
1. **MVP 版本**：
   - 上传 + OpenCV.js 透视校正 + A4 标定
   - SAM 一键分割 + 基础轮廓提取
   - 轮廓偏移 + 简单拉伸 3D + STL 导出
   - 3D 预览

2. **V1.1 版本**：
   - 轮廓编辑器完善
   - 手指取放槽
   - DXF/SVG 导出
   - 多工具同板排列

3. **V2.0 版本**：
   - Gridfinity 底座生成
   - STEP 导出（OCCT B-Rep）
   - 用户账号 + 云端保存项目
   - 批量处理