调研完成。基于 Tooltrace.ai 和 Tracemate 的对标分析，加上 Web 端技术可行性验证，下面是优化后的完整产品开发需求文档，可直接用于开发或写 PRD。

# 📐 工具轮廓扫描 & 3D 收纳生成器 — 产品需求文档（PRD）

## 基本信息

- **对标产品**：Tooltrace.ai / Tracemate
- **平台**：Web 浏览器（Chrome 优先）
- **核心技术**：OpenCV.js + SAM（Segment Anything）+ OpenCASCADE WASM

## 一、产品概述

### 1.1 一句话定位

用户将工具平放在 A4/Letter 纸上俯拍上传，系统自动识别纸张标定尺寸、AI 提取工具轮廓、配置嵌件参数，一键导出 STL / STEP / DXF / SVG，用于 3D 打印或激光切割工具收纳嵌件。

### 1.2 目标用户

- 3D 打印爱好者 / Maker
- 工厂车间工具管理（5S / Kaizen 泡沫嵌件）
- Gridfinity 收纳系统用户

### 1.3 核心价值

从"拍照"到"可打印模型"，全程浏览器完成，无需安装软件、无需上传云端，保护用户隐私。

## 二、用户流程（6 步向导）

```
Step 1: 上传照片 ──→ Step 2: 纸张检测与标定 ──→ Step 3: AI 工具轮廓提取
                      │
Step 6: 下载文件 ←── Step 5: 导出预览 ←── Step 4: 轮廓编辑 & 嵌件参数配置
```

## 三、功能需求详述

### 3.1 图像采集规范（引导页）

| 要求     | 说明                                          |
| -------- | --------------------------------------------- |
| 背景     | 白色/浅色 A4 或 Letter 纸                     |
| 拍摄角度 | 尽量正俯视，距离远 + 光学变焦（减少透视畸变） |
| 光照     | 均匀漫射光，避免工具投下浓重阴影              |
| 工具摆放 | 工具间留间距 ≥ 5mm，不重叠                    |
| 支持格式 | JPG / PNG / WebP                              |

📌 关键提示：引导页需告知用户"远距离 + 变焦"拍摄，避免视差误差（parallax error）。

### 3.2 纸张检测与透视校正

| 功能                   | 技术方案                                                    |
| ---------------------- | ----------------------------------------------------------- |
| 自动检测纸张四角       | OpenCV.js 边缘检测 + 四边形拟合                             |
| 透视校正（Homography） | 将歪斜的纸张 warp 为正射俯视图                              |
| 尺寸标定               | 已知 A4=210×297mm 或 Letter=215.9×279.4mm，计算像素/mm 比例 |
| 手动修正               | 若自动检测失败，用户可手动拖拽四角                          |
| 纸张格式选择           | 下拉选择 A4 / A5 / Letter，默认 A4                          |

### 3.3 AI 工具轮廓提取

| 功能         | 技术方案                                                             |
| ------------ | -------------------------------------------------------------------- |
| 一键 AI 分割 | 集成 SAM（Segment Anything）ONNX 模型，浏览器 WASM + Web Worker 运行 |
| 交互方式     | 点击工具区域 → 实时生成掩码 → 提取外轮廓                             |
| 多工具支持   | 每张照片可添加多个工具，逐个点击识别                                 |
| 备选方案     | 传统 CV 方案（Canny + 形态学 + findContours）作为轻量 fallback       |
| 模型缓存     | SAM 权重首次下载后缓存至 IndexedDB / OPFS，约 40-100MB               |

### 3.4 轮廓编辑器

| 功能           | 说明                                    |
| -------------- | --------------------------------------- |
| 矢量预览       | SVG Canvas 展示轮廓路径（毫米坐标）     |
| 拖点微调       | 拖动轮廓顶点修正 AI 误差                |
| 增/删点        | Ctrl+点击加点的，Ctrl+点击点删除        |
| 平滑处理       | 高斯/Chaikin 平滑去噪                   |
| 旋转           | 拖拽旋转手柄调整工具方向                |
| 复制/删除      | 多工具管理                              |
| 偏移（Offset） | 轮廓向内/外偏移 0~2mm，用于补偿打印公差 |

### 3.5 嵌件参数配置

| 参数            | 默认值              | 说明                                      |
| --------------- | ------------------- | ----------------------------------------- |
| 轮廓偏移        | +0.3mm（外扩）      | 补偿打印机公差，确保工具能放入            |
| 底板厚度        | 2mm                 | 嵌件底部支撑厚度                          |
| 腔体深度        | 15mm                | 工具嵌入深度                              |
| 手指取放槽      | 自动/手动           | 在轮廓边缘凹处生成半圆/椭圆凹槽，深度可设 |
| 布局模式        | 单工具 / 多工具同板 | 多工具时自动排列打包                      |
| Gridfinity 模式 | 可选                | 生成标准 42mm Gridfinity 底座网格         |

### 3.6 3D 模型生成与预览

| 功能     | 技术方案                                                      |
| -------- | ------------------------------------------------------------- |
| 矢量→3D  | 闭合多边形拉伸（Extrude）生成实体                             |
| 布尔运算 | 腔体挖空 + 手指槽切割                                         |
| 几何内核 | OpenCASCADE WASM（B-Rep 实体建模，支持 STEP 导出）或 Manifold |
| 3D 预览  | Three.js 渲染，支持旋转/缩放                                  |
| 尺寸标注 | 显示工具轮廓的实际毫米尺寸                                    |

### 3.7 导出格式

| 格式      | 用途                                                |
| --------- | --------------------------------------------------- |
| STL       | 直接切片 3D 打印（最常用）                          |
| STEP      | 导入 Fusion 360 / SolidWorks 二次编辑（B-Rep 保留） |
| 3MF       | 多色/多 Body 打印                                   |
| DXF / SVG | 激光切割泡沫或亚克力衬板                            |

## 四、技术架构

```
┌─────────────────────────────────────────────┐
│ 前端（React + TypeScript）                   │
│ ┌─────────┬──────────┬──────────┬─────────┐ │
│ │ 上传向导 │ 轮廓编辑器 │ 3D 预览 │ 导出面板 │ │
│ └────┬─────┴────┬─────┴────┬─────┴────┬────┘ │
│      │          │          │          │      │
│ ┌────▼────┐ ┌───▼───┐ ┌───▼───┐ ┌────▼────┐ │
│ │OpenCV.js│ │ SAM   │ │Three.js│ │OCCT WASM│ │
│ │透视校正  │ │AI分割 │ │渲染引擎 │ │B-Rep建模│ │
│ │轮廓提取  │ │       │ │       │ │STEP导出 │ │
│ └─────────┘ └───────┘ └───────┘ └─────────┘ │
│      │          │          │                 │
│      └──────────┴──────────┴─────────────────┘ │
│                Web Worker 层                  │
│ （SAM 推理 / OCCT 计算 不阻塞 UI）            │
└─────────────────────────────────────────────┘
```

### 技术选型表

| 模块     | 推荐方案                             | 说明                               |
| -------- | ------------------------------------ | ---------------------------------- |
| 框架     | React + TypeScript                   | 对标产品均用 React                 |
| 图像校正 | OpenCV.js                            | 文档扫描级透视校正                 |
| AI 分割  | SAM ONNX (量化版) + ONNX Runtime Web | 官方支持 WASM + SIMD + Web Worker  |
| 矢量处理 | Paper.js / Clipper2 WASM             | 轮廓偏移、布尔运算                 |
| 3D 建模  | OpenCASCADE WASM (occt-wasm)         | 唯一浏览器端 B-Rep 内核，支持 STEP |
| 3D 预览  | Three.js                             | 标准 Web 3D 渲染                   |
| 状态管理 | Zustand / Jotai                      | 轻量响应式                         |
| 构建工具 | Vite                                 | 原生支持 WASM                      |

## 五、非功能性需求

| 维度   | 要求                                                  |
| ------ | ----------------------------------------------------- |
| 性能   | SAM 推理 < 3s（量化模型 + WebGPU 优先）；3D 生成 < 2s |
| 浏览器 | Chrome/Edge 最新版优先，Firefox 兼容，Safari 部分支持 |
| 隐私   | 所有计算在浏览器本地完成，不上传图片到服务器          |
| 离线   | Service Worker 缓存核心资源，支持弱网使用             |
| 响应式 | 桌面端为主，编辑器区域适配 1366px+                    |

## 六、开发优先级建议（MVP → V2）

### MVP（4-6 周）

- ✅ 上传 + OpenCV.js 透视校正 + A4 标定
- ✅ SAM 一键分割 + 基础轮廓提取
- ✅ 轮廓偏移 + 简单拉伸 3D + STL 导出
- ✅ 3D 预览

### V1.1

- ✅ 轮廓编辑器（拖点/平滑/旋转）
- ✅ 手指取放槽
- ✅ DXF / SVG 导出
- ✅ 多工具同板排列

### V2.0

- ✅ Gridfinity 底座生成
- ✅ STEP 导出（OCCT B-Rep）
- ✅ 用户账号 + 云端保存项目
- ✅ 批量处理

## 七、关键风险与应对

| 风险                                 | 应对策略                                               |
| ------------------------------------ | ------------------------------------------------------ |
| SAM 模型体积大（~100MB），首次加载慢 | 分模型档位（Tiny/Small/Base），按需下载；OPFS 持久缓存 |
| 手机广角拍摄透视畸变严重             | 引导页强调"远距离+变焦"；提供 ArUco 角标增强标定方案   |
| 复杂工具（钳子/扳手）投影轮廓不完整  | 允许用户手动绘制补充轮廓；提供"多层嵌件"选项           |
| WASM 内存限制                        | 大模型推理放 Web Worker；复杂场景分步处理              |

---

Web 端 CAD/AI 工具一定要"前端先行"，先把交互骨架搭起来，后面每个功能模块（CV / AI / 3D）都是"插拔式"的。

下面是一个可直接开工的开发方案 + 项目脚手架结构，从 Web 搭建 → UI 骨架 → 逐步接入 OpenCV / SAM / 3D，每一步都可独立运行、可演示。

# 🛠️ Web 端「工具轮廓扫描 & 收纳生成器」开发方案（前端先行版）

## 一、总体开发策略（重点）

- ✅ 前端优先：先不管算法，先用假数据把 UI/UX 跑通
- ✅ 模块化：图像 / 矢量 / 3D 各自独立，后期替换实现
- ✅ 渐进增强：每一步都能打开浏览器看到效果
- ✅ 本地优先：所有重计算放 Web Worker，UI 不卡顿

## 二、技术栈（推荐，已验证可行）

| 层级    | 技术                                      |
| ------- | ----------------------------------------- |
| 框架    | React 18 + TypeScript + Vite              |
| 样式    | Tailwind CSS（快速搭 UI）                 |
| 状态    | Zustand（简单、适合工具类应用）           |
| 画布    | Fabric.js（SVG 编辑，比原生 Canvas 省心） |
| 3D 预览 | Three.js + @react-three/fiber             |
| CV / AI | OpenCV.js（WASM）、SAM ONNX（后期）       |
| 构建    | Vite（原生支持 WASM、Worker）             |

✅ 为什么选 Fabric.js？

因为你需要：拖点、缩放、旋转、平滑、偏移，Fabric 内置 SVG Path 编辑，非常适合轮廓编辑器。

## 三、项目初始化（第一步）

### 1️⃣ 创建项目

```bash
npm create vite@latest tooltrace-web -- --template react-ts
cd tooltrace-web
npm install
```

### 2️⃣ 安装基础依赖

```bash
npm install tailwindcss postcss autoprefixer
npx tailwindcss init -p

npm install fabric zustand
npm install three @react-three/fiber @react-three/drei
```

### 3️⃣ Tailwind 基础配置（tailwind.config.js）

```js
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

在 src/index.css：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

## 四、推荐目录结构（非常重要）

```
src/
├── app/
│   └── store.ts # 全局状态（Zustand）
│
├── features/
│   ├── upload/ # Step 1：上传图片
│   ├── calibration/ # Step 2：纸张标定（预留）
│   ├── segmentation/ # Step 3：AI 分割（预留）
│   ├── editor/ # Step 4：轮廓编辑（核心）
│   ├── params/ # Step 5：嵌件参数
│   └── export/ # Step 6：导出（预留）
│
├── components/
│   ├── Layout.tsx
│   ├── Stepper.tsx # 顶部步骤条
│   └── CanvasEditor.tsx # Fabric.js 编辑器
│
├── workers/ # Web Worker（后期）
│   └── cv.worker.ts
│
├── pages/
│   └── Home.tsx
│
└── main.tsx
```

## 五、先做「最小可用前端」（MVP UI）

### ✅ 目标

- 6 步向导 UI
- 图片上传
- 一个 SVG 画布（Fabric.js）
- 一个"假轮廓"（模拟 AI 结果）
- 参数面板
- 3D 预览占位

## 六、Step 1：全局状态设计（Zustand）

src/app/store.ts

```ts
import { create } from "zustand";

type Step =
  | "upload"
  | "calibration"
  | "segmentation"
  | "editor"
  | "params"
  | "export";

interface AppState {
  step: Step;
  setStep: (step: Step) => void;

  imageUrl: string | null;
  setImageUrl: (url: string) => void;

  contours: any[]; // 后期替换为真实轮廓类型
  setContours: (c: any[]) => void;
}

export const useStore = create<AppState>((set) => ({
  step: "upload",
  setStep: (step) => set({ step }),

  imageUrl: null,
  setImageUrl: (imageUrl) => set({ imageUrl }),

  contours: [],
  setContours: (contours) => set({ contours }),
}));
```

## 七、Step 2：顶部步骤条（Stepper）

src/components/Stepper.tsx

```tsx
import { useStore } from "@/app/store";

const steps = [
  "upload",
  "calibration",
  "segmentation",
  "editor",
  "params",
  "export",
];

export function Stepper() {
  const step = useStore((s) => s.step);
  const setStep = useStore((s) => s.setStep);

  return (
    <div className="flex gap-2 p-4 bg-gray-100">
      {steps.map((s, i) => (
        <button
          key={s}
          onClick={() => setStep(s as any)}
          className={`px-3 py-1 rounded ${
            step === s ? "bg-blue-600 text-white" : "bg-white"
          }`}
        >
          {i + 1}. {s}
        </button>
      ))}
    </div>
  );
}
```

## 八、Step 3：图片上传页

src/features/upload/UploadPage.tsx

```tsx
import { useStore } from "@/app/store";

export function UploadPage() {
  const setImageUrl = useStore((s) => s.setImageUrl);
  const setStep = useStore((s) => s.setStep);

  const onFile = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file) return;
    setImageUrl(URL.createObjectURL(file));
    setStep("editor"); // 暂时跳到 editor
  };

  return (
    <div className="p-8">
      <h2 className="text-xl mb-4">上传工具照片</h2>
      <input type="file" accept="image/*" onChange={onFile} />
    </div>
  );
}
```

## 九、Step 4：Fabric.js 轮廓编辑器（核心）

src/components/CanvasEditor.tsx

```tsx
import { useEffect, useRef } from "react";
import { fabric } from "fabric";
import { useStore } from "@/app/store";

export function CanvasEditor() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const imageUrl = useStore((s) => s.imageUrl);
  const setContours = useStore((s) => s.setContours);

  useEffect(() => {
    if (!canvasRef.current || !imageUrl) return;

    const canvas = new fabric.Canvas(canvasRef.current, {
      width: 800,
      height: 600,
      backgroundColor: "#eee",
    });

    // 加载图片
    fabric.Image.fromURL(imageUrl, (img) => {
      img.scaleToWidth(800);
      canvas.add(img);

      // ✅ 假轮廓（模拟 AI 结果）
      const fakePath = new fabric.Path(
        "M 200 150 L 400 150 L 450 300 L 150 300 Z",
        {
          fill: "rgba(0,150,255,0.3)",
          stroke: "blue",
          strokeWidth: 2,
          selectable: true,
        },
      );

      canvas.add(fakePath);
      setContours([fakePath]);
    });

    return () => canvas.dispose();
  }, [imageUrl]);

  return <canvas ref={canvasRef} />;
}
```

👉 这一步做完，你已经有一个"能拖拽轮廓"的编辑器了

## 十、Step 5：参数面板（占位）

src/features/params/ParamsPanel.tsx

```tsx
export function ParamsPanel() {
  return (
    <div className="p-4 w-64 border-l">
      <h3 className="font-bold mb-2">嵌件参数</h3>

      <label className="block mb-2">
        轮廓偏移 (mm)
        <input className="border w-full px-2" defaultValue={0.3} />
      </label>

      <label className="block mb-2">
        底板厚度 (mm)
        <input className="border w-full px-2" defaultValue={2} />
      </label>

      <label className="block mb-2">
        腔体深度 (mm)
        <input className="border w-full px-2" defaultValue={15} />
      </label>
    </div>
  );
}
```

## 十一、首页组装

src/pages/Home.tsx

```tsx
import { useStore } from "@/app/store";
import { Stepper } from "@/components/Stepper";
import { UploadPage } from "@/features/upload/UploadPage";
import { CanvasEditor } from "@/components/CanvasEditor";
import { ParamsPanel } from "@/features/params/ParamsPanel";

export function Home() {
  const step = useStore((s) => s.step);
  const imageUrl = useStore((s) => s.imageUrl);

  return (
    <div className="h-screen flex flex-col">
      <Stepper />

      <div className="flex flex-1 overflow-hidden">
        <div className="flex-1 overflow-auto">
          {!imageUrl && <UploadPage />}
          {imageUrl && <CanvasEditor />}
        </div>

        {imageUrl && <ParamsPanel />}
      </div>
    </div>
  );
}
```

## 十二、你现在拥有的"成果"

- ✅ Web 项目骨架
- ✅ 6 步向导 UI
- ✅ 图片上传
- ✅ Fabric.js SVG 编辑器（可拖点、缩放、旋转）
- ✅ 参数面板
- ✅ 状态管理（为后续算法接入留好接口）

后面每一周，只需要"换掉假数据"即可：

| 阶段   | 替换内容                             |
| ------ | ------------------------------------ |
| 下周   | OpenCV.js 透视校正（替换假图片处理） |
| 再下周 | SAM 分割（替换假轮廓）               |
| 再下周 | Clipper.js 轮廓偏移                  |
| 再下周 | Three.js + OCCT 生成 STL             |

## 新增功能模块建议

考虑到项目的需求，在后续规划中建议增加以下功能模块：

### 文件导出功能的具体实现

1. 实现多种格式导出功能（STL/STEP/3MF/DXF/SVG）
2. 添加导出进度指示器
3. 提供导出文件预览功能

### 错误处理和用户提示机制

1. 统一错误处理框架
2. 用户友好的提示信息
3. 操作状态反馈机制
4. 异常恢复机制

### 性能优化策略

1. Web Workers实现后台计算
2. 模型缓存机制（IndexedDB/OPFS）
3. 按需加载大体积模型
4. 内存使用优化

### 响应式设计的具体实现细节

1. 桌面端优化布局
2. 移动端适配方案
3. 不同屏幕尺寸适配
4. 触摸设备交互优化

### 测试策略

1. **单元测试**
   - 使用Vitest/Jest进行逻辑测试
   - 核心算法函数测试

2. **组件测试**
   - 使用React Testing Library
   - UI组件交互测试

3. **端到端测试**
   - 使用Cypress/Playwright
   - 完整用户流程测试
